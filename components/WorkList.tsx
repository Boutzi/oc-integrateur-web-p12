import { Section } from "./Section";

export const WorkList = () => {
  return (
    <Section>
      <h1 className="font-caption text-5xl font-bold text-primary max-md:text-4xl max-sm:text-3xl">Past jobs</h1>
      <p>...jobs</p>
    </Section>
  );
};
